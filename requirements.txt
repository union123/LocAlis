# ============================================================
#  Мультиагентная локально-первая платформа — зависимости
#  Проверено на Python 3.10.6 (Windows 11).
#  ВАЖНО: Python 3.13/3.14 не поддерживается — нет колёс faiss-cpu
#  и части экосистемы langchain. Используйте venv на 3.10-3.12.
# ============================================================

# --- ядро графа и агентов --------------------------------------------------
langgraph>=0.2.60,<0.5          # оркестрация узлов (Router -> ... -> Judge)
langchain-core>=0.3.30,<0.4     # базовые типы сообщений/раннаблов

# --- единый шлюз к моделям -------------------------------------------------
litellm>=1.55.0                 # unified gateway: ollama + openrouter одним API
openai>=1.60.0                  # OpenAI-совместимый клиент для OpenRouter
ollama>=0.4.5                   # нативный клиент Ollama (tool calling)

# --- устойчивость ----------------------------------------------------------
pybreaker>=1.2.0                # Circuit Breaker для облачных вызовов
httpx>=0.27.0                   # health-check с таймаутом
tenacity>=8.2.0                 # ретраи локальных вызовов

# --- контракты и конфигурация ---------------------------------------------
pydantic>=2.9,<3                # все схемы обмена между агентами
pydantic-settings>=2.6.0
PyYAML>=6.0.1                   # config/*.yaml

# --- retrieval -------------------------------------------------------------
faiss-cpu>=1.8.0                # векторный индекс (CPU, не занимает VRAM)
numpy>=1.26,<3

# --- хранилище и миграции --------------------------------------------------
# SQLite входит в стандартную библиотеку; alembic — для будущих сложных миграций
SQLAlchemy>=2.0.36
alembic>=1.14.0

# --- observability ---------------------------------------------------------
# SDK версии 2.x совместим с сервером langfuse:2 из
# scripts/langfuse-docker-compose.yml. SDK 4.x требует сервер v3 —
# при обновлении меняйте оба места одновременно.
langfuse>=2.57.0,<3             # трейсинг (self-hosted через Docker)

# --- инструменты (плагины из tools/) ---------------------------------------
# Ставятся сюда, а не в манифесты: pip не умеет доустанавливать зависимости
# плагина на лету, а неготовый инструмент честно показывается как
# «окружение не готово» на экране «Инструменты».
pypdf>=5.1.0                    # tools/files — чтение PDF (конспекты, статьи)
matplotlib>=3.9.0               # tools/charts — графики в PNG (режим Agg)
openpyxl>=3.1.5                 # tools/datafiles — чтение и запись Excel
python-docx>=1.1.2              # tools/datafiles — отчёты Word

# --- интерфейс -------------------------------------------------------------
nicegui>=2.9.0                  # desktop-панель управления (русский UI)
# streamlit>=1.41.0             # альтернатива NiceGUI, при желании раскомментировать

# --- тесты -----------------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0

# ============================================================
#  PyQGIS НЕ ставится через pip.
#  Он входит в состав QGIS Desktop и работает только под
#  интерпретатором QGIS. Плагин tools/qgis/ вызывает его
#  через subprocess (bin/python-qgis-ltr.bat + worker.py),
#  поэтому venv платформы остаётся чистым.
#  Установка: https://qgis.org/download  (нужен QGIS 3.28+)
# ============================================================
